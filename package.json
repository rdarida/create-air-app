{
  "name": "create-air-app-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "setup": "npm install && npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "format": "prettier --write \"packages/*/lib/**/*.js\"",
    "publish": "lerna publish"
  },
  "author": "Róbert Darida",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  }
}
