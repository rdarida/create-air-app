{
  "name": "create-air-app-monorepo",
  "private": true,
  "author": "Róbert Darida",
  "license": "MIT",
  "scripts": {
    "setup": "npm install && npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean",
    "format": "prettier --write \"packages/*/src/**/*.{js,ts}\"",
    "publish": "lerna publish"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  }
}
